# Production Environment Configuration
# Used with Docker Compose + Nginx reverse proxy

# TX API - Proxied through Nginx at /api/v1
VITE_TX_API_URL=/api/v1

# TX Analyzer - Proxied through Nginx at /api/v1
VITE_TX_ANALYZER_URL=/api/v1

# WebSocket - Proxied through Nginx at /ws
VITE_WS_URL=/ws
