# MSQ Transaction Monitor - Frontend Environment Configuration
# Copy this file to .env.development or .env.production and configure for your environment

# =============================================================================
# API Endpoints
# =============================================================================

# TX API Base URL (Transactions & Addresses endpoints)
# Development (direct): http://localhost:8000/api/v1
# Production (nginx):   /api/v1
VITE_TX_API_URL=/api/v1

# TX Analyzer Base URL (Analytics & Statistics endpoints)
# Development (direct): http://localhost:8002/api/v1
# Production (nginx):   /api/v1
VITE_TX_ANALYZER_URL=/api/v1

# =============================================================================
# WebSocket Configuration
# =============================================================================

# WebSocket URL for real-time transaction updates
# Development (direct): ws://localhost:8001
# Production (nginx):   /ws (auto-detects ws:// or wss://)
VITE_WS_URL=/ws

# =============================================================================
# Notes
# =============================================================================
# - VITE_ prefix is required for Vite to expose variables to client code
# - Relative paths (starting with /) work with nginx reverse proxy
# - Absolute URLs (http://...) are needed for direct service execution
# - WebSocket URL will auto-detect protocol (ws/wss) for relative paths
#
# Execution Modes:
# 1. Docker Compose + Nginx: Use relative paths (default in .env.production)
# 2. Direct Execution (pnpm dev): Use absolute URLs (default in .env.development)
