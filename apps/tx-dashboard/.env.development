# Development Environment Configuration
# Used when running services directly with pnpm dev (without Docker Compose)

# TX API - Runs on port 8000
VITE_TX_API_URL=http://localhost:8000/api/v1

# TX Analyzer - Runs on port 8002
VITE_TX_ANALYZER_URL=http://localhost:8002/api/v1

# WebSocket - Chain Scanner runs on port 8001
VITE_WS_URL=ws://localhost:8001
